#import <UIKit/UIKit.h>

/* Native navigation controller for a player session. Keeps the status bar hidden
 * while presented and restores the host's legacy visibility on dismissal. */
@interface RDLPPlayerNavigationController : UINavigationController
@end
